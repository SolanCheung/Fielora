# Agent context, request compatibility and composer drafts

2026-09-10. User-reported flow: attach screenshots to an existing conversation, retain an unsent draft while switching conversations, and diagnose a small UI defect without repeated broad reads or misleading completion.

Confirmed evidence: the prior conversation request and the later no-tool assistant response were followed by HTTP 400. The compatible adapter emits `tool_calls: []` on ordinary assistant messages, an avoidable wire compatibility defect; the provider's historical response body was not retained, so this is not proof of its exact rejection reason. The long run compressed history five times and received actual provider usage; its first 56 calls exhausted the two-million cumulative input allowance. Initial repository excerpts remained pinned, and repeated reads/searches were not sufficiently distinguished from progress. Switching conversations explicitly cleared text and attachments.

Reuse Model + Harness + Tools. Correct optional wire fields and classify bounded rejection bodies into fixed categories without retaining text, credentials or response payloads. Reduce speculative initial context, retain task and image requirements, summarize useful tool evidence when trimming old complete exchanges, and recognize repeated/no-result observations. Keep cumulative usage separate from active context size; do not raise the budget or mistake compaction for task completion. UI drafts are session-local editing state keyed by Project and Conversation, including asynchronous attachment completion; they do not replace persisted messages or add a Core truth store.

No migration, permissions expansion, external model calls, or edits to either finance project. Changes are reversible and historical runs/receipts remain readable. Validate compatible-provider fixture requests for history plus images and no-tool turns, failure categorization, draft switching/late uploads, reduced context and preserved evidence, Cross lane and packaged desktop smoke. Real Qwen behavior remains subject to interactive acceptance.

Implementation refinement: ResumeAgentRunRequest adds optional attachments for legacy recovery, validated by the existing image constraints. New run inputs reuse ContentBlobStore plus a descriptor-only Checkpoint; existing inputs must match and corrupted/missing durable bytes fail closed. No schema change. search_text now returns actual hit context for minified translation dictionaries; this was directly responsible for hiding the requested field names from model observations.
