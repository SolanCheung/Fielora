# Precise source observation — 2026-09-13

The latest production continuation performed 49 calls. Browser blocker facts enabled opening the modal, but a resize requested before its run-owned page existed was discarded. Reading the 64 KB one-line translation dictionary raised individual model input to 60k tokens. Regex alternatives searched that line with find(), exposing only its first match while truncated=false. The model repeatedly reread the template and finally proposed changing shared export translations; the existing guard rejected that mutation.

Correct Tools within existing ToolRuntime: enumerate all bounded occurrences, retain exact UTF-8 locations and honest caps; read_file gains bounded byte windows and JSON Pointer selection through the same project/sensitive-file controls and whole-file hash. Default raw output is bounded before appending a long line, with explicit continuation metadata. Output redaction preserves source whitespace and CRLF instead of collapsing indentation or JSON string spaces; existing sensitive-line/token masking remains. Desktop regression also requires deferring native viewport emulation until a committed loaded document exists. No schema/migration/credential/permission change. Preserve distinct lookup/window evidence during Harness coalescing/compaction. Do not promote source text or model interpretation to task completion.

Browser viewport intent becomes order-independent inside existing ephemeral run/page ownership: resize may store viewport intent before open without creating a tab, explicitly reporting no page/document observation; subsequent open creates the page with that viewport. No unrelated tab adoption, server action, site CSS rewrite or user-data mutation.

Validate against a synthetic dictionary matching the actual long-line structure, all alternative hits, missing pointers, UTF-8 window continuation and protected paths; replay the actual relevant key offsets/values without business edits. Exercise real Desktop packaged resize-before-open and source lookup through the model message path. Deterministic fixtures cannot establish Qwen semantic convergence. No configured Coding Plan automated acceptance.

Packaged regression also exposed a FRAME_NOT_READY input preflight. Unifying capture options and retries did not fix it; those experiments were not delivered as a proven repair.

The full packaged fixture then exposed the concrete error `Current display surface not available for capture`. A screenshot is not an input precondition. Native click now retains two current DOM/hit checks and acknowledged Chromium input, without making PNG capture a prerequisite. Do not force a covered target or treat input acknowledgment as success of the task; post-input observation and required rendered verification remain. Native fixture event counters and seven rendered fields must pass under the same multi-page scenario.

The remaining white surface/native input failure was isolated without Model/Core/business code. Native repro documents remained hidden inside a visible, focused window. Disabling only CalculateNativeWinOcclusion restored all three pages' PNG capture and trusted clicks; disabling emulation, changing initial view visibility, detaching inactive views and disabling active-page background throttling did not. Main now disables that Windows-only OS heuristic before Chromium startup, retaining other flags, explicit page/window visibility, default background throttling, sandbox and DOM hit protection. Covered active windows may render more often; power consumption is unmeasured. No dependency/schema/permission change. Full development and packaged regressions remain required; this does not prove Qwen completes the original task.
