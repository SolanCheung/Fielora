# Plan recovery correction — 2026-09-11

The real resumed receipt-popup run produced 32 quote mismatches and a stale-source rejection, then continued implementing its own invented customer-title requirement. Exact quotation and a model-authored file list did not establish that the task was understood. The previous mandatory work-plan gate therefore added failure loops without proving correctness.

Make work_plan an optional, revisable intent record in the existing tool ledger. Direct edits no longer require a plan or membership in its model-authored file list. Existing project/permission policy, guarded write hashes, shared-translation impact checks, and current-result verification remain enforced. No permission expansion, schema change, second runtime or resource-budget increase. Plans, including old persisted plans, never establish user requirements or permission.

Resolve optional source references against bounded read receipts, normalize display line numbers, and refresh still-matching quotes after unrelated edits. Invalid or stale references are reported as evidence issues and excluded from confirmed facts, without rejecting the entire intent record. Source observations and model interpretations remain separate. A plan update cannot satisfy completion or count as new execution progress.

On continuation, explicitly compare carried edits with the original task/images before filling dependencies of model-added features. Keep this recovery instruction outside the compacted history. Report malformed edit arguments with their exact schema and a safe structural example. Present planning and evidence issues distinctly in the execution history.

Validate multiline quotations, self-edit refresh, wrong references, actual source changes, direct writes without a plan and outside its advisory scope, old-plan resume, guarded-write conflicts, and rendered acceptance after restart in an isolated fixture. Cross and targeted packaged regressions; fixture completion does not certify a real model's semantic judgment. Do not modify the finance projects or invoke a live provider.

Validated: Cross 243 TS / 312 Rust / 13 integration, UI/UX 23, development and packaged recovery/browser fixture, packaged goal lifecycle and historical image recovery PASS. Actual Qwen/business acceptance remains unverified. Delivery: artifacts/agent-plan-recovery-packaged/DELIVERY.md.
