# Explicit continuation and recovered operational facts — 2026-09-11

Confirmed production run resumed at step 171 with 1,894,974 previously charged input tokens, no resource renewal, and paused after four requests at 2,024,231. Previous pause reason was verification, not budget, so the old reason-dependent renewal missed this continuation. Images were present in all four calls. Browser status lost process ownership across app restart although a local service still listened at 127.0.0.2:8019. Checkpoints discarded server command/status information, repeated an unmarked resume instruction, and promoted model work-plan criteria into goal acceptance cases.

Within the existing Harness, every trusted explicit user resume starts a fresh bounded time/token allowance and records the prior pause reason in the existing atomic RunResumed event. Historical usage remains in the ledger; cumulative steps and completion obligations are unchanged. Automatic retries/checkpoints/restarts do not grant resources. No migration or second accounting system.

Recover bounded historical server commands and actual browser/process results from ToolCall receipts. Treat absent host process tracking as unknown ownership, not service absence. Permit browser_server status with an explicit loopback URL to inspect an independently running endpoint without adopting, stopping or restarting it. Existing loopback validation and permissions remain. No application request, process adoption or automatic execution of historical commands.

Deduplicate resume instructions, demote legacy checkpoint plan criteria, and retain failure/status facts across compaction. Original task/images remain authoritative; a model's plan is not the user's acceptance specification. UI labels elapsed time as cumulative. Validate near-budget non-budget pauses through explicit resume and Core restart, context boundedness, actual external local listener ownership, and packaged desktop continuity. No production project edits or real-provider evaluation.
