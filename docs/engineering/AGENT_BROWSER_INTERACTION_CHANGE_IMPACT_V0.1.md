# Browser interaction receipts and recovery — 2026-09-12

The production run's click of an observed invoice anchor returned UNKNOWN in 9 ms. Its host error/dispatch details were discarded, and resume classified the NETWORK tool as unreplayable and failed the entire run. The original cause of this click cannot be recovered. Current DOM visibility includes off-canvas links, preflight exceptions cross Electron as wrapped errors, and the host guesses outcome from the requested action. These are confirmed implementation defects; off-canvas hit testing is a reproduction hypothesis.

Extend the existing browser tool receipt with explicit input dispatch state. Fixed DOM preflight failures must return structured errors; native dispatch and later observation are separate phases. A delivered input followed by an observation failure requires fresh inspection, never automatic replay. A genuinely uncertain dispatch or lost transport response remains UNKNOWN, retains available host diagnostics, and cannot become verified success. Legacy unknown browser mutations remain subject to manual reconciliation, but retry must not falsely convert an unresolved run into a terminal failure. Read-only browser observations can be safely reconciled separately from mutating NETWORK operations.

Expose bounded observed HTTP(S) anchor addresses and viewport/hit-test facts so the model can reveal collapsed navigation or navigate an observed link. No arbitrary JavaScript, forced clicks through overlays, credential input, new permission presets, schema or state store. Existing navigation policy and native input remain authoritative. This repairs the built-in browser tool, not full Windows computer use.

Validate in real narrow Electron WebContentsView: covered/off-canvas targets dispatch no input; stale targets retain their error; revealing navigation and clicking a link reaches the target; post-input observation loss does not replay input; modal checks and screenshot evidence complete. Keep uncertain financial side effects blocked and preserve original requirements across continuation. Run regression in dev and packaged host. Fixtures establish tool/orchestration behavior, not real-model task competence. Production database is read-only and production business services/files remain untouched.

Native input regression also requires the containing Electron BrowserWindow and page to acquire focus before preflight, followed by mouse move/down/up. Electron documents focus as a prerequisite for sendInputEvent; an unfocused dispatch may return without affecting the page. Verification must inspect the actual resulting UI.

The isolated HTTP Electron probe confirms a further input-readiness defect: a visible, focused DOM produces no native mouse or keyboard effect until a bounded capturePage read synchronizes its compositor surface. Data-URL-only fixtures and tests that capture a verification screenshot before clicking mask this behavior. Add a bounded, discarded frame read followed by fresh preflight before native click, and fixed acknowledged Chromium mouse commands. No pixels are retained/uploaded and no arbitrary debugger API is exposed. Focus alone and changing input API alone did not resolve the observed failure.
