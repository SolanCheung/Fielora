# 当前请求上下文与历史执行证据

日期：2026-09-17；范围：用户已授权的上下文修正。

流程：修改失败 → 追问原因 → 读取同一对话的历史执行证据并回答 → 用户明确要求后继续修改。当前原因解释的完成与旧实现任务的完成分开。

复用现有 Model + Harness + Tools、Conversation/Run/Event/ToolCall/ContextSnapshot。Ingress & Context 提供有界历史索引、当前原始请求与来源，每次模型调用刷新当前请求边界；Continuity 从原账本按需读取历史工具事实；Orchestration 修正“改成功”命中“改成”的具体假阳性，记录结束判定原因。保持主模型理解语言，不增加常驻分类 Agent、强制计划、第二套状态或权限。

新增内置只读 `read_run_history`，通过既有工具提议、Policy、执行、回执链路。只可读取当前 Project/Conversation、当前原消息边界之前的 Run；分页、有界输出，错误/未知与成功分开，内容是历史事实而非授权、当前验证或重放指令。不继承参考路径写权限，不扩大旧 reference_task 文件读取范围，不返回凭据、完整工具参数、命令输出或模型推理。

无 FIPC/DTO/SQLite Migration。已有消息和事件不重写；上下文投影在恢复时重建。新增事件 metadata 向后兼容；旧包不会使用新上下文工具。逐轮记录来源 ID、选取数量、截断、消息/系统/工具哈希与完成判定，避免保存原始敏感 prompt。

验证：原句和混合要求的边界回归；历史索引/按需回执分页；跨 Project/Conversation/未来消息拒绝；恢复与压缩后当前请求保留；真实 Core/Electron 中失败→原因回答零写入→显式修复及验证；已有未验证修改仍不能假完成。运行 Cross 与定向桌面/打包回归。确定性模型替身验证链路，不声明 Qwen 真实语义质量或原业务已解决；真实模型须使用合规凭据且独立报告。
