# Search and observation continuity repair — 2026-09-12

Production Run 01a09553-7a96-7f82-baa5-b78b40157b9c made 56 calls (34 search, 21 read, 1 list), no edits or browser checks. Nine regex-like queries failed under the literal-only contract. Thirteen generic replan notices and seven context reductions did not change the model's diagnosis. Current template fallback text also differs from the dictionary values selected by its translation keys; no evidence supports the model's repeated claim of another modal.

User flow: continue a screenshot-reported coding fix, retain already observed source facts, test remaining hypotheses and verify the current result. Extend existing search_text with bounded Rust regex matching and explicit literal/regex options. For legacy regex-like queries, prefer actual literal matches; if none, disclose regex fallback in the receipt. Invalid patterns return actionable errors, not missing-code evidence. Preserve project path, sensitive-file and result limits; expose scan omissions and matched coordinates. No shell regex execution, write authority or schema change. regex 1.12.2 is pinned; builder memory/nesting limits follow https://docs.rs/regex/1.12.2/regex/struct.RegexBuilder.html.

Within existing WorkProgress and context reduction, distinguish repeated observations from novel results; changing result limits or rereading overlapping ranges is not new evidence. Replan context lists repeated source/query observations and missing result checks, rather than accumulating identical notices. Compaction deduplicates observations by source/result identity and excludes repeated model diagnosis from retained facts. No mandatory plan, write allowlist, forced edits, new state store, higher budget or automatic verification. Unchanged reads still run against the filesystem; no stale cache substitutes for current evidence.

Compatibility: old receipts retain their fallback novelty calculation; new fields are additive. Existing default literal hits and literal:true remain literal. Only regex-like queries with zero literal hits gain the explicitly reported fallback. Original task/images, complete recent tool exchanges, failure evidence and normal permission/verification gates remain intact. Rollback is source-only; no migration.

Verify actual production query patterns on isolated sources, literal punctuation, invalid/oversized regex, Unicode/minified text, result caps and excluded files. Replay receipt-shaped search/read cycles including changed versions and overlapping/subset results; verify focused recovery survives compaction without inventing goal completion. Cross plus existing desktop goal/browser regressions and packaged smoke. No live provider automation or business writes; fixtures cannot certify model semantic convergence.
