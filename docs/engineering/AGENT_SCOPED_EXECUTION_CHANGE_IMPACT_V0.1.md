# Scoped diagnosis and verification — 2026-09-11

Flow: compare a reference popup with the target, correct local labels/order while retaining existing data/API behavior, then verify that view. Actual run 01a08f9f-f962-7f31-a549-cab2496b35e0 received two images in all 60 calls and reduced context 11 times, but performed 45 reads/searches and no checks. It misused literal search, overlooked the directive's existing bank loader, and reassigned a shared translation key used by other pages.

Reuse Model + Harness + Tools, existing ToolCall/receipt/checkpoint persistence and browser verification. Add a bounded work_plan tool for scope, expected results, preserved behavior, observed source quotes and next action. Quotes require actual current read receipts; interpretations remain model proposals, never verification or permission. Keep this plan and its source evidence in compaction/restart context. UI writes require an acceptance plan; a declared file scope constrains subsequent direct edits. Shared translation changes surface outside consumers before writes and require those consumers to be included in the reviewed scope. Plan amendments require a reason and new observed evidence; they do not grant authority or prove semantic completeness.

Distinguish awaiting verification from actually checking. Drive feedback from outstanding acceptance and changes, not generic eight-step review narration. Count overlapping reads and identical search locations as repeated evidence, not fresh progress merely because arguments differ. Improve search/Git argument errors with exact recovery contracts; retain literal semantics and all existing permission boundaries. No new durable schema, provider binding, second state machine, higher budgets or automatic real-provider calls. Do not edit the user's finance projects.

Validate adversarial attempts: mutation before a plan, shared-key collateral changes, scope drift, invented/stale evidence, repeated reads, ambiguous regex-like searches, duplicated Git subcommands, premature finish and failed UI checks. Use an isolated popup fixture with an existing loader and shared translation consumer; prove local correction, unchanged API/shared consumer and actual browser acceptance. Cross plus packaged regressions; deterministic tests are not real-model quality certification.

Validation completed: Cross 242 TS / 310 Rust / 13 integration, UI/UX 23; development and packaged scoped/browser checks plus packaged image/draft and goal-lifecycle regressions PASS. See artifacts/agent-scoped-execution-packaged-r2/DELIVERY.md. The initial runtime-catalog registration omission was detected in packaged testing and corrected before delivery.
