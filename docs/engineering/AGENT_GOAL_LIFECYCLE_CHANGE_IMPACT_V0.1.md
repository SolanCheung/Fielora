# Goal-driven Agent lifecycle — 2026-09-11

Flow: continue a screenshot-based UI correction in the same conversation; preserve the original inputs, finish outstanding work and verify the result without a hidden 16-step termination.

Confirmed: run 01a08e9b-f722-7d13-b644-030825825469 used FOCUSED_EDIT, stopped at 16 despite max_steps=4096, received zero images, and had no browser tools. Its sole replace_text failed without writing. The previous correction covered General continuity and same-run image recovery, not this combination.

Reuse Model + Harness + Tools and existing AgentRun/ToolCall/Checkpoint/VerificationReceipt. Strategies choose methods, not independent completion authority. Focused shares continuous execution, resource accounting, context reduction, feedback and fresh verification. A bounded fast strategy that cannot establish the result yields its evidence to the shared loop in the same Run. Unknown side effects, denied authority, unavailable required inputs, persistent no-progress or global resource boundaries pause with an explicit cause. Do not raise cumulative budgets or equate an operation count/model assertion with goal completion.

Derive progress and remaining obligations from the original task, supplied/reference images, actual changes and current verification receipts. Unverified finish attempts return concrete remaining work to the model; only persistent unchanged attempts pause. UI requirements require the existing browser acceptance path when available. Preserve failed tests and current-revision checks; model-generated acceptance cases still require user review for semantic completeness.

Carry referenced historical images only within the selected conversation, using the existing bounded attachment store and run input blobs. Missing explicitly referenced images prevent blind execution. No new database schema, permission escalation, second runtime, credential use or edits to the user's finance projects. Test focused work beyond 16 steps, recoverable edits, historical image follow-up, fresh/no-change/failed/stale verification, meaningful progress versus repeated claims, explicit budget pause, fast fallback and packaged browser/continuation workflows. Real-provider acceptance remains separate.

Historical-image recovery amendment: actual run 01a08f9f-f962-7f31-a549-cab2496b35e0 was a retry of a text-only reference to a legacy gallery. Retry supplied zero images; the missing-input guard paused before any model call. Share the conversation resolver with retry/resume, bounded by the originating message so later galleries cannot replace the original reference. A modern run may fill previously missing references only while paused at step zero for AGENT_REFERENCED_IMAGES_UNAVAILABLE, with no model invocation or tool calls. Existing persisted inputs remain immutable; ordinary text-only runs cannot gain images on resume. Reuse the input blob checkpoint and preserve the pause reason; no schema or permission change. Exercise legacy gallery → failed text-only reference → UI retry and missing-reference pause → UI resume after restart, including rejection of unrelated late inputs.
