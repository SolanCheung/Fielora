# 历史图片追问修复 — 2026-09-18

- 用户流程：同一对话上传报表截图，随后询问“你知道图片里面的需求是什么吗”“我最近发给你的这张”，仍能查看最近图库。
- 已确认：生产报表 Run 的 blob 完整、1920×912，模型输入有 1 张图；两次追问的所有模型请求无图片。Core 和旧附件恢复使用双关键词门槛。最初看错删除标记不能归因于缺图，需单独评估模型理解。
- 修正：Core 对无新附件的请求按同 Project/Conversation 和原消息边界恢复最近有界图库，不依赖用户措辞。保留来源消息/运行身份，作为历史证据，不续期旧任务。当前明确附件优先；图库按原上传消息排序，旧任务的新重试不成为新上传；缺失或损坏的最近图库不降级选旧图。现有 legacy UI 路径保留兼容。
- 所有权：既有 Harness Ingress & Context / Continuity、Run/Event/Blob；无新 Runtime、Schema、Provider 分支、权限扩张。恢复的是已进入同一对话的图片，不扫描项目 PNG。
- 兼容：旧 manifest 可读；新 checkpoint 只增加来源元数据；重启/重试保持原消息上界。普通无图对话不凭空附图。最多 4 图、现有字节限制不变。
- 验证：生产只读元数据与 blob 哈希；原话的连续追问、当前新图优先、跨会话/未来消息排除、损坏不替换、重启和压缩图片保留、实际 Provider-neutral 请求图片指纹；开发/打包桌面回归。替身不证明 Qwen 正确理解红线，原报表不在本次修改范围。
