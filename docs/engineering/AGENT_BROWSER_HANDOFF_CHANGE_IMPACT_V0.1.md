# Browser observation and login handoff — 2026-09-12

The latest real Run resumed at step 230 and made 54 model requests (2,003,473 input tokens) before pausing at 284. Context reductions and both original images were present. Its successful navigation returned EMPTY at a login route before asynchronous rendering; no later inspect occurred. Repeated process status returned the same 8,192-character build tail. WorkspaceRuntime stops updating that tail when terminal emission reaches 512 KiB, so old build progress survives after actual page readiness. These are confirmed tool-feedback defects, alongside the model's repeated source reads and unsupported completion proposals.

Preserve the terminal display emission cap while continuously retaining a bounded latest output tail. For read-only browser observations, wait a bounded interval for initial asynchronous rendering and refresh page identity; do not retry mutating actions. Preserve loaded-page facts across context reduction, and explicitly distinguish password visibility from login authorization or verification.

Add request_login to the existing browser tool: the model can request a user handoff only against a fresh snapshot with an observed visible password input. The host rechecks the DOM; the existing RunPaused event records AGENT_BROWSER_LOGIN_REQUIRED after durable tool receipts. No credentials are read, filled, stored or requested in chat. User completes login in the existing browser and explicitly resumes the same Run; its next observation must recheck the page before verification. Merely encountering a password form does not automatically pause login-page development tasks. No new state store, migration, permission preset or verification authority. No automatic completion or enlarged budget.

Validate output beyond the emission cap, delayed SPA rendering/route changes, blank-page rejection, stale handoff rejection, password redaction, pause without another model call, user completion/resume and actual page checks in dev and packaged Electron. Keep production database read-only, production business files and services untouched; synthetic fixtures do not establish real-model semantic accuracy.
