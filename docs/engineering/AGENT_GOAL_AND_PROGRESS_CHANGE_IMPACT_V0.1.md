# Goal closure and stable progress repair — 2026-09-10

Observed General run `01a08966-190f-7170-aad0-96c4636e0dd3` completed after 21
read/search operations with verification_passed=false. The mutation phrase 改成
was absent from action detection. Target binding was dalieyingcai, while model
text called it the reference headhunt-web project. Earlier failed attempts had
persisted writes, so failure never implied rollback.

Reuse Work Scope & Goal, Ingress & Context, Continuity and Verification/Evidence.
No schema, permission, tool exposure or new Runtime. Unify change/action intent,
provide explicit target identity and same-project/same-task historical facts as
unverified context. Mutation goals require current-state verification; already
satisfied goals may finish without artificial edits only after a check bound to
the current inspected files. Missing evidence pauses, never reports success.
Historical failures remain immutable and do not grant current verification.

Presentation pins narrative above operations, merges adjacent inspection steps
without crossing approval/phase boundaries, differentiates search/read/directory
icons, preserves raw chronology in grouped details, and renders decorative result
checkmarks using neutral shared glyphs without changing stored/copyable content.

Validate false-completion rejection, verified no-change and stale-file refusal,
target/reference context, streaming/durable handoff geometry, grouping/history,
result glyphs and actual packaged Electron. No real provider automation.
